import './Aurora.css';

/**
 * Aurora background (Epic 9). Soft, static blue/purple/mint gradients at very
 * low opacity. Decorative only — never overlaps or interferes with content.
 */
export function Aurora() {
  return <div className="lm-aurora" aria-hidden="true" />;
}
